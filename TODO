== soon and important ==
* add documentation

== later and important ==
* exporter

== not so important ==
* nicer design
* upgraded navigation (back-buttons, all sort of cross-links)
* page estimates
* nicer admin interface
* display captions during page listing
* tweaked print.css
* webaccess to rcs history
* trail export (to view the current length)
* interactive page swapping
* create manual for setting up a local dokuforge server
* escaping in ednotes in exportparser
* sensible file types for downloads
* "wrong password" needs html rendering
* Adminstration raises exceptions on bad input
* blob edit forgets form fields on error
* blob edit should mention filename restrictions
* blob edit complains about invalid chars on valid long inputs
* blob edit complains about invalid chars on valid long inputs
* extremely long course titles break layout
* course/title -> abort is a loop
* academy/title -> abort is a loop
* course/aca permit whitespace only titles
